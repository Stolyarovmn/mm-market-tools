name: Daily Action Plan

on:
  schedule:
    - cron: '0 6 * * 1-5'
  workflow_dispatch:

# Этот workflow теперь только триггер для напоминания.
# Реальный сбор данных — локально через run_daily.bat (RU IP required).
jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Data collection runs locally
        run: echo "Data collection runs locally via run_daily.bat. See README."
